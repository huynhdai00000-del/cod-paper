# Does the surrogate flatten the thermal cycle?

Evaluation set: **T1 in-distribution — frozen fix-7 sampler (`fc4cb76c3b32ec17`), held-out seed 999**, n = 40. Truth by RK45 on `fast_rhs_np` at `rtol = 1e-9`, 100 query points across the window. Generated by `audit_port/scripts/18_swing_fidelity.py`.

**Question.** Neural networks are spectrally biased toward low frequencies, so a thermal surrogate may smooth peaks. If it does it discards part of the Jensen gap the method exists to preserve, and **thermal MAE would not show it** — a flattened trajectory can be close in mean absolute error while systematically under-stating the peak-to-trough range the convex Arrhenius integral is sensitive to. Because `V_arr` is convex, the error is also signed: a positive temperature excursion adds more gap than an equal negative one removes, so a model unbiased in degrees is not unbiased in aging rate. §4 is where that shows up.

**The mechanism under test.** COD predicts a correction to an analytic first-order solution, so the cycle shape comes from the IEC baseline and the network's spectral bias has nothing to flatten. Any architecture *without* that baseline has to generate the cycle from the network itself, which is exactly the frequency content spectral bias suppresses. So the prediction is not "neural surrogates flatten cycles" but "neural surrogates flatten cycles **when nothing else supplies the shape**".

## 1. Headline

Half peak-to-peak of the hot-spot trajectory. Live cases are time-varying with a true swing above 1 degC; constant-load windows have no cycle to preserve and no Jensen gap by definition (N-4).

| model | analytic baseline H | checkpoint | n live | median swing ratio | under-predicting | median thermal MAE degC | median thermal bias degC |
|---|---|---|---|---|---|---|---|
| ExactModel (zero error) | n/a | `16_bias_diagnosis.ExactModel` | 38 | **1.0000** | 71% | 0.0000 | -0.0000 |

## 2. Full distribution of the swing ratio, per model

| model | population | n | median ratio | Q1 | Q3 | p10 | p90 | median error degC |
|---|---|---|---|---|---|---|---|---|
| ExactModel (zero error) | all cases | 40 | **1.0000** | 1.0000 | 1.0000 | 1.0000 | 1.0000 | -0.0000 |
| ExactModel (zero error) | time-varying | 38 | **1.0000** | 1.0000 | 1.0000 | 1.0000 | 1.0000 | -0.0000 |
| ExactModel (zero error) | time-varying, swing > 1 degC | 38 | **1.0000** | 1.0000 | 1.0000 | 1.0000 | 1.0000 | -0.0000 |
| ExactModel (zero error) | constant K | 2 | **1.0000** | 1.0000 | 1.0000 | 1.0000 | 1.0000 | +0.0000 |

## 3. Stratified by true swing — does it worsen where it matters?

**This is the table that decides whether a given thermal MAE is acceptable.** A spectral-bias failure shows up as a ratio below 1 that **worsens as the swing grows**. A ratio near 1 that is flat across the bands is the absence of the failure; a ratio that falls with the band is the failure itself. A band whose MAE is above 5 degC is not evidence either way — the model is not tracking the trajectory there, and its swing ratio says nothing about spectral bias (N-9).

| model | true swing band | n | median ratio | median error degC | median MAE degC | tracking |
|---|---|---|---|---|---|---|
| ExactModel (zero error) | 1-5 degC | 3 | 1.0000 | -0.0000 | 0.0000 | yes |
| ExactModel (zero error) | 5-10 degC | 9 | 1.0000 | -0.0000 | 0.0000 | yes |
| ExactModel (zero error) | 10-15 degC | 11 | 1.0000 | -0.0000 | 0.0000 | yes |
| ExactModel (zero error) | 15-25 degC | 10 | 1.0000 | -0.0000 | 0.0000 | yes |
| ExactModel (zero error) | 25-200 degC | 5 | 1.0000 | -0.0000 | 0.0000 | yes |

### 3b. By sampler family

Which load families the result is actually about. `family` is the day-pattern the case was drawn from; `kind` is the *realised* in-window variation, so a family that varies over the day can still appear as a constant window (fix 7 made that a real part of the population).

| model | family | n | n live | median true swing degC | median ratio | median MAE degC |
|---|---|---|---|---|---|---|
| ExactModel (zero error) | `base_load` | 9 | 9 | 11.64 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `daily` | 14 | 14 | 17.55 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `evening_peak` | 5 | 5 | 12.90 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `multi_step` | 3 | 3 | 7.40 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `overload_spike` | 3 | 1 | 15.97 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `ramp` | 3 | 3 | 5.90 | 1.0000 | 0.0000 |
| ExactModel (zero error) | `shift_change` | 3 | 3 | 9.36 | 1.0000 | 0.0000 |

## 4. The consequence that MAE hides

The reason to measure the swing rather than the error: the Jensen gap carried by the predicted trajectory against the gap carried by the true one. Any flattening shows up here even when MAE is small. The last column is signed so that **negative means gap lost**, i.e. the predicted trajectory carries less Arrhenius acceleration than the true one.

| model | state | median true gap | median predicted gap | median ratio | delta gap, ratio of medians |
|---|---|---|---|---|---|
| ExactModel (zero error) | `c_H2` | 1.3568 | 1.3568 | 1.0000 | +0.00% |
| ExactModel (zero error) | `c_C2H2` | 2.0140 | 2.0140 | 1.0000 | -0.00% |
| ExactModel (zero error) | `c_C2H4` | 1.5639 | 1.5639 | 1.0000 | +0.00% |
| ExactModel (zero error) | `c_CO` | 1.2078 | 1.2078 | 1.0000 | +0.00% |
| ExactModel (zero error) | `c_CO2` | 1.1465 | 1.1465 | 1.0000 | +0.00% |
| ExactModel (zero error) | `DP` | 1.4483 | 1.4483 | 1.0000 | +0.00% |

Thermal MAE over the live cases, for context:

| model | median MAE degC | p90 MAE degC | median bias degC |
|---|---|---|---|
| ExactModel (zero error) | 0.0000 | 0.0000 | -0.0000 |

MAE and peak-to-trough range are not the same measurement, and only one of them is what the convexity argument depends on. Reading the two tables together is the point: a model can be close in MAE and still lose gap, and the gap column is the one the method's claim rests on.

## 5. Gate

Fails if any band the model tracks (median MAE <= 5 degC) has a median swing ratio below 0.95.

**PASS** — the cycle is preserved in all 5 band(s) the model tracks.

## 6. What this does not establish

1. **Ablation A is still the test this approximates.** Ablation A is COD's architecture with the analytic baseline replaced by the constant `x0` — same network, same pipeline, one variable. Its weights are not among the supplied artifacts (`cod/models/cod.py`, `CODNoBaseline`). The monolithic pair removes the analytic baseline **and** the cascaded gas integral and was trained to a far worse optimum, so it is a two-variable substitute. DECISIONS O-12 is the one training run that would settle it.

2. **Every monolithic checkpoint carries the J-8 defect** — its thermal exponent was shadowed to 12 instead of 0.8 during training — and none of them converged (N-9). Their swing ratios are reported for continuity with the earlier version of this document, not as evidence about architecture.

3. **The v57 and fix-7 rows are not comparable.** They are different models scored on different distributions; only rows sharing an evaluation set can be read against each other.

